{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "prepare": "node ./configure-references.js",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "test": "yarn vitest"
  },
  "devDependencies": {
    "eslint": "8.40.0",
    "lerna": "6.6.2",
    "rimraf": "5.0.1",
    "typescript": "5.0.4",
    "vitest": "0.31.1"
  }
}
